<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_list_path, class: "btn btn-primary btn-sm pull-right") %>
    <%= link_to("Delete", list_path(@list), class: "btn btn-sm btn-default pull-right", method: :delete, data:{confirm: "Are you sure?"}) %>
  </div>
  <h2><%= @list.title %></h2>
  <%= @list.description %>
</div>